import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF6C5CE7); // vivid purple
  static const Color accent = Color(0xFF00C9A7); // teal
  static const Color warm = Color(0xFFFFD166); // warm yellow
  static const Color background = Color(0xFFF7F6FF);
  static const Color cardShadow = Color.fromRGBO(0, 0, 0, 0.06);
}
